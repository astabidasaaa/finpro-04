export type Address = {
  id: number;
  name: string;
  address: string;
  zipCode: number;
  latitude: string;
  longitude: string;
  isMain: boolean;
};

export type TLocation = {
  lat: number;
  lng: number;
};
